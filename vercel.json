{
  "buildCommand": "npm run vercel-build || npm run vercel-build-fallback",
  "installCommand": "npm install",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  },
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}